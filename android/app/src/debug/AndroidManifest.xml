<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alkalab.ourworld">

    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.alkalab.ourworld.MainApplication"
        android:label="@string/appName"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:name,android:label">  <!-- Add this line to replace conflicting attributes -->

        <!-- Add the tools:replace for the AD_SERVICES_CONFIG conflict -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource"/>

        <!-- Other application elements go here -->
    </application>

</manifest>
